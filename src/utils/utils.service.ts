// import { Dispatch } from "@reduxjs/toolkit";

export class Utils {
  // static dispatchNotification(message: string, type: string, dispatch: Dispatch) {
  //     dispatch(addNotification({ message, type }));
  //   }
}
