import styled from "styled-components";
import { motion } from "framer-motion";

export const AdminChatStyles = styled(motion.section)`
  display: flex;
  width: 100%;
  aside {
    width: 30%;
  }
  main {
    width: 70%;
  }
`;
